interface IPerson {
  id: number;
  firstName: string;
  lastName: string;
  dateOfBirth: string;
  height: number;
}
const id = "id";
const firstName = "firstName";
const lastName = "lastName";
const dateOfBirth = "dateOfBirth";
const height = "height";

export { IPerson, id, firstName, lastName, dateOfBirth, height };
